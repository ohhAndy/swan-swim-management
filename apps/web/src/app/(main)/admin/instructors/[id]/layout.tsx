import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit Instructor | Swan Swim Management",
};

export default function EditInstructorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
